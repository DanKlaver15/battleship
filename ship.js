"use strict";

class Ship {
	constructor(name, initials, size) {
		this.name = name;
		this.initials = initials;
		this.size = size;
	}
}

/*======================================================================*/

module.exports = {
   Ship: Ship
}
